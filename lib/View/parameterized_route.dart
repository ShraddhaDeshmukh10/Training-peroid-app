import 'package:flutter/material.dart';

class Parameterized_Route extends StatelessWidget {
  const Parameterized_Route({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          backgroundColor: Colors.blue,
          title: Text("Welcome !!!! Parameterized Route"),
        ),
        body: Text("Hello I am Shraddha Deshmukh"));
  }
}
